import { TableChooserDirective } from './table-chooser.directive';

describe('TableChooserDirective', () => {
  it('should create an instance', () => {
    const directive = new TableChooserDirective();
    expect(directive).toBeTruthy();
  });
});
